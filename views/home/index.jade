//- index.jade
extends ../layout.jade

block head
  title= title

block content
  h1= title

  table
    tr
      th username
    each user in users
      tr
        td
          a(href="/" + user.username)= user.username