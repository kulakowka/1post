form.commentForm.js-form-comment
  input(type="hidden" name="parentId" placeholder="parentId" value=parentId)
  .field
    textarea.js-textarea-autosize(name="textSource" rows="1" placeholder="Enter your message" maxLength="100000" required value=textSource)
  .field
    button.btn.btn-submit(type="submit") publish